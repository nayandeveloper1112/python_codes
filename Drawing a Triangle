import turtle

# Set up the turtle
t = turtle.Turtle()
t.speed(1)

# Draw a triangle
for _ in range(3):
    t.forward(100)  # Move forward by 100 units
    t.left(120)     # Turn left by 120 degrees

# Complete the drawing
turtle.done()
